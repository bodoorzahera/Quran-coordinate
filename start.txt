Option 1: Clone the repo
git clone https://github.com/quran/quran.com-images.git

# Then run
python3 generate_coords.py --build-db

# Option 2: If you already have it somewhere else, specify the path
python3 generate_coords.py -q /path/to/quran.com-images --build-db
python3 server.py --images-dir ./images --json-dir ./output --port 8003


python3 server.py --images-dir ./images --json-dir ./output --mushaf-dir ./mushaf --port 8008
